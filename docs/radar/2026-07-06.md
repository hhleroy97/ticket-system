# RADAR 2026-07-06

## Provenance Graph Not Built
**Files:** `scripts/provenance_graph.py`, `scripts/github_intel.py`, `docs/index.json`
**Rationale:** `docs/index.json` has no `graph` section. Run `github_intel.py` after `scan.py` so RADAR, docgen, and reach queries can use commit/PR/co-change edges.
**Acceptance:** index.json schema_version >= 3 with non-empty graph.nodes after intel refresh.

## Stale High-Churn Modules With Quiet Dependents
**Files:** `scan.py`, `docs/index.json`
**Rationale:** These modules have elevated commit counts while indexed importers show fewer commits, suggesting dependent code may not have kept pace with hotspot changes.

## Dependency Graph Coverage Looks Adequate
**Files:** `docs/index.json`, `scan.py`
**Rationale:** The index reports 56 import edge(s) across 27 production Python module(s), enough for basic dependency tracing.

## Production Python Modules Lack Test Import Edges
**Files:** `run_tests.py`, `scripts/graph_delta.py`, `scripts/reach_query.py`, `tests/test_agent_plan.py`, `tests/test_agent_progress.py`, `tests/test_cursor_agent.py`, `tests/test_dashboard.py`, `docs/index.json`
**Rationale:** Indexed tests do not import these production modules, so churn and dependency signals may not reflect test coverage gaps.
