# RADAR 2026-06-13

## No Stale High-Churn Modules Detected
**Files:** `docs/index.json`
**Rationale:** `docs/index.json` reports `total_commits: 1`, and every indexed file has `commits: 1` with `last_commit: 2026-06-13`, so the current index does not contain a high-churn module or older dependent to flag as stale.

## Dependency Graph Has No Edges
**Files:** `docs/index.json`, `scan.py`, `docgen.py`, `draft_issues.py`, `tests/test_scan.py`
**Rationale:** `docs/index.json` reports `edge_count: 0` and an empty `edges` list, which means dependency-risk and dependent-recency analysis cannot currently trace relationships among indexed Python modules such as `scan.py`, `docgen.py`, and `draft_issues.py`.

## Dashboard Generation Lacks Focused Tests
**Files:** `docgen.py`, `templates/dashboard.html.tmpl`, `docs/dashboard.html`, `tests/test_docgen.py`
**Rationale:** `tests/test_docgen.py` verifies module documentation output, but the indexed dashboard template and generated `docs/dashboard.html` are not directly asserted, leaving dashboard rendering and index-field usage vulnerable to unnoticed drift.

## Test Runner Module Lacks Direct Tests
**Files:** `run_tests.py`, `tests/test_docgen.py`, `tests/test_scan.py`, `tests/test_hooks.py`
**Rationale:** `run_tests.py` is an indexed Python module, while the indexed test files cover doc generation, scanner behavior, and hook syntax rather than test discovery or exit-code behavior for the runner itself.

## Radar Workflow Automation Lacks Local Validation
**Files:** `.github/workflows/radar.yml`, `.github/workflows/radar-tickets.yml`, `.github/workflows/executor.yml`, `draft_issues.py`, `docs/radar/README.md`
**Rationale:** The index includes multiple radar and executor workflow definitions, but the local tests only exercise `draft_issues.py` parsing through `tests/test_docgen.py`; workflow invocation, labels, permissions, and artifact handoff are not covered by an indexed local test.
